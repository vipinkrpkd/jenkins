pi peline { 
agent any 
    stages {  
        stage ('Build') { 
		steps {
			echo "Build done...!"
		}
        }
        stage ('Test') { 
		steps{ echo "Test done" }
        
        }
       
        stage ('Deploy') { 
        	steps{ echo "Deploy done "}
        }
 
    }           
 }
